# Server Configuration
# Port number for the Express server
PORT=5000

# Database Configuration
# MongoDB connection string
MONGODB_URI=MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/tic-tac-toe

# Environment
# Options: development, production
NODE_ENV=development

# Frontend Configuration
# Production frontend URL for CORS (optional)
# If not set, only local development URLs will be allowed
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Note: CORS origins are configured in src/config/index.js
# Default allowed origins:
# - http://localhost:3000 (local frontend)
# - http://127.0.0.1:3000 (local frontend alternative)
# Additional origins can be added via FRONTEND_URL environment variable